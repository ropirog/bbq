<div class="event-photo buffer-both">
  <%= link_to image_tag(photo.photo.variant(:thumb)), url_for(photo.photo) %>
  <%= t("views.add") %> <%= render partial: "users/user", object: photo.user %>
</div>
